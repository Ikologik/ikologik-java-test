spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/ikologik-java-test
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASS:root}